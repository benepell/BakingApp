<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/recipe_widget_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:contentDescription="@string/recipe_widget_name"
        android:paddingBottom="8dp"
        android:visibility="invisible" />

    <ListView
        android:id="@+id/widget_listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBackgroundTextList"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/widget_text_error"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
       android:background="@color/colorPrimary"
        android:textColor="@color/blue_A700"
        android:gravity="center"
        style="@style/TextAppearance.AppCompat.Large"
        android:contentDescription="@string/widget_text_error"
        android:visibility="gone"/>


</LinearLayout>